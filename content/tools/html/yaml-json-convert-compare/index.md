---
date: 2026-01-30
draft: false
title: 'YAML ↔ JSON Convert & Compare'
description: 'Convert YAML to JSON (and back), or compare two files for logical equality with formatting controls.'

resources:
  - name: tool-file
    src: tool.html
  - name: tool-icon
    src: images/tool-icon.svg

tags:
  - html
  - yaml
  - json
  - convert
  - compare
---
# YAML ↔ JSON Convert & Compare

{{< tool-image >}}

Convert YAML to JSON or JSON to YAML, or compare two documents for logical equality. Includes formatting controls, copy/save actions, and shareable state.

{{< tool-link link_text="Open the tool" >}}

## Notes

- YAML comments are ignored during conversion.
- Multiple YAML documents in a single file are rejected.

## Dependencies

{{< tool-dependencies >}}
